import * as actions from '../actions/buttonModal';

const initialState = {

};

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
